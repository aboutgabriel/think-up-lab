

## Aumentar o mockup dos dispositivos

Aumentar o `max-w` do container do mockup de `880px` para `1050px`, permitindo que a imagem ocupe mais espaço na tela.

**Arquivo:** `src/components/ui/DevicesMockup.tsx`
- Linha 5: alterar `max-w-[880px]` para `max-w-[1050px]`

